image

function preload(){

}

function setup() {
  let myCanvas = createCanvas(innerWidth, innerHeight);
  myCanvas.parent("mainSketch");
  background(0);
  let middleOfCanvas = (innerWidth/2) - ()
}
